
export const mapbox = (
    accessToken,
    mapStyle,
) => {
    console.log(accessToken)
    console.log(mapStyle)
    // mapboxgl.accessToken = accessToken;
    // var map = new mapboxgl.Map({
    //     container: 'map', 
    //     style: mapStyle,
    //     center: [0,0], 
    //     zoom: 9
    // });
    // console.log(map)
}